--------------Taint-----------------------------
kubectl taint nodes node1 app=db:NoSchedule
kubectl taint nodes node1 app=db:NoSchedule-
---------------Tolerations-----------------------
apiVersion: v1
kind: Pod
metadata:
  name: pod-with-toleration
spec:
  tolerations:
    - key: "app"
      operator: "Equal"
      value: "db"
      effect: "NoSchedule"
  containers:
    - name: nginx
      image: nginx
