Ubuntu 24.0 - kubeadm cluster
----
apt update && apt upgrade -y
swapoff -a
sed -i '/ swap / s/^/#/' /etc/fstab

modprobe overlay
modprobe br_netfilter

tee /etc/sysctl.d/k8s.conf <<EOF
net.bridge.bridge-nf-call-ip6tables = 1
net.bridge.bridge-nf-call-iptables = 1
net.ipv4.ip_forward = 1
EOF
sysctl --system
-----------------------------------------------containerd--------------------------------
apt install -y containerd
mkdir -p /etc/containerd
containerd config default | tee /etc/containerd/config.toml
---------------# We set SystemdCgroup=true to make containerd use the same cgroup(put limit on resorces like cpu,ram,...) driver as Kubernetes (systemd)----
nano /etc/containerd/config.toml
	[plugins."io.containerd.grpc.v1.cri".containerd.runtimes.runc.options]
  	SystemdCgroup = true
------------------
systemctl restart containerd
systemctl enable containerd
----------------------------------------------------------------------------------------
apt install -y apt-transport-https ca-certificates curl
curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.34/deb/Release.key | gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
echo 'deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.34/deb/ /' | tee /etc/apt/sources.list.d/kubernetes.list

apt update

apt-get install -y kubelet="1.34.0-*" kubectl="1.34.0-*" kubeadm="1.34.0-*"
apt-mark hold kubelet kubeadm kubectl

kubeadm init --pod-network-cidr=192.168.0.0/16

mkdir -p $HOME/.kube
cp -i /etc/kubernetes/admin.conf $HOME/.kube/config
chown $(id -u):$(id -g) $HOME/.kube/config

kubectl apply -f https://raw.githubusercontent.com/projectcalico/calico/v3.26.0/manifests/calico.yaml

kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml

====> metrics-server.yaml
----
kubeadm join 10.0.0.10:6443 --token xxxxx.yyyyy \
    --discovery-token-ca-cert-hash sha256:zzzzzzzz
------------------------------------------------------nodes-------------------------------------------------------
apt update && apt upgrade -y
swapoff -a
sed -i '/ swap / s/^/#/' /etc/fstab
modprobe overlay
modprobe br_netfilter
tee /etc/sysctl.d/k8s.conf <<EOF
net.bridge.bridge-nf-call-ip6tables = 1
net.bridge.bridge-nf-call-iptables = 1
net.ipv4.ip_forward = 1
EOF

sysctl --system

apt install -y containerd
mkdir -p /etc/containerd
containerd config default | tee /etc/containerd/config.toml
nano /etc/containerd/config.toml
	SystemdCgroup = true

systemctl restart containerd
systemctl enable containerd

apt install -y apt-transport-https ca-certificates curl
curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.34/deb/Release.key | gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg
echo 'deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.34/deb/ /' | tee /etc/apt/sources.list.d/kubernetes.list

apt update
apt-get install -y kubelet="1.34.0-*" kubectl="1.34.0-*" kubeadm="1.34.0-*"
apt-mark hold kubelet kubeadm kubectl

Server(controle plane): kubeadm token create --print-join-command
kubeadm join 172.20.200.197:6443 --token 017m1n.8om1clc9kp40c8e2 --discovery-token-ca-cert-hash sha256:f6fdc721fe55feba14ed9c75881dd63d94a3386fda8e1a8bdf098d34ef7a4c6b



